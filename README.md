# password-generator
Description: A Python-based application that generates strong and secure passwords based on user input. The user can specify the desired password length, and the program creates a random combination of uppercase letters, lowercase letters, numbers, and special characters. 
